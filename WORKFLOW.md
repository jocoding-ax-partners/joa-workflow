# Feature Workflow

> joa 플러그인의 방법론 정본. 스킬(`joa:spec` ~ `joa:loop`)이 이 문서의 각 단계를
> 집행하고, 통과 판정은 `bin/joa.mjs` 가 한다.

기능 하나를 처음부터 끝까지 만들 때의 순서. 화면 없는 기능이면 2·3 을 건너뛴다.
자잘한 수정에는 쓰지 않는다 — 판단 기준은 "계획 문서가 필요한가".

## 1. 기획 초안 (end-to-end)

정해진 것 없이 전반을 논의한다. **끝까지 훑되 확정하지 않는다.**

다룰 것: 목적 · 필요한 것 · **오버엔지니어링 검문**(정말 필요한가, 더 작게 되나) ·
개발 스펙 · 프레임워크 · 인프라/백엔드의 방향(상세는 5단계).

- 산출물: `docs/specs/<기능>.md` 초안 한 장
- 게이트: 사용자 승인
- 병렬: ✗ · 질문은 한 번에 하나

**Why:** 5단계의 병렬 에이전트들이 같은 그림을 보려면 여기서 end-to-end 가 한 번 그려져 있어야 한다.

## 2. ASCII 와이어프레임

- **가로 180~200자.** 좁으면 디테일이 죽고 넓으면 안 읽힌다.
- **한 문서에 전 화면을 다 넣는다.** 화면별로 쪼개지 않는다 — 쪼개면 화면 사이 흐름이 문서 사이로 사라진다.
- **와이어프레임마다 end-to-end 동작 설명 한 문단.** 어떻게 들어와서, 뭘 누르면, 어디로 가는가.

- 산출물: `docs/specs/<기능>-wireframe.md` 한 장
- 게이트: 사용자가 배치·구성 확정

**Why:** 이 단계에서 정할 것은 "무엇이 어디에 있는가" 하나. HTML 로 바로 가면 레이아웃 논의가 CSS 논의로 오염된다.

## 3. Claude Design → HTML Export

**`frontend-design` 스킬이 아니다.** 2단계 문서를 통째로 Claude Design 에 넣어 디자인을 뽑고,
Export 해서 HTML 을 받는다. 전 화면이 한 문서이고 화면마다 동작 설명이 붙어 있는 것이 여기서
값을 한다 — Design 이 화면 간 일관성과 흐름을 같이 본다.

- 산출물: Export 된 HTML · 게이트: 육안

## 4. 프론트 구현 + mock API

3단계 HTML 을 실제 프론트로 옮긴다.

스택 규칙(상태관리·UI 라이브러리·아키텍처 슬라이스·컴포넌트 분리 기준)은 프로젝트의
`.claude/rules/*.md` 에 박아두고 "규칙 읽고 그대로"만 시킨다.
**브리프에 규칙 본문을 복붙하지 않는다** — 두 벌이 되면 갈라진다.

- 산출물: 동작 화면 + mock 핸들러
- 게이트: mock 으로 화면이 끝까지 걸어진다
- 누가: 서브에이전트 · 병렬: 화면 단위 ✓

**Why:** mock 핸들러가 곧 "백엔드가 뭘 줘야 화면이 그려지는가"의 실측이고, 그게 5단계 입력이다.

## 5. 백엔드·인프라 상세 플랜 — 한 단계, 3막

### 막 1 — 계약 동결 (직렬, 메인 스레드 직접)

4단계 mock 을 한 곳의 타입 정의로 승격한다: 요청·응답 스키마 · **에러 코드 집합** ·
상태 머신 · 각 필드의 소유자(누가 계산하는가).

- 게이트: 프론트가 mock 을 걷고 계약 타입으로 타입체크 통과
- 위임 금지 — 정의상 단일 writer

**Why:** 에러 코드 집합을 빠뜨리면 나중에 모든 실패가 "네트워크를 확인하세요"로 뭉개진다.
계약이 없으면 병렬 기획 A·B 가 서로 떠넘기거나 둘 다 만든다.

### 막 2 — 병렬 상세 플랜

- 산출물: 계획 문서 여러 장 · 누가: 서브에이전트 병렬
- **병렬 기준은 주제가 다른 것이 아니라 쓰기 경로 교집합이 공집합인 것.**
  마이그레이션 번호·라우트 파일·테이블을 미리 배분한다. 겹치면 직렬.
- 각 에이전트는 막 1 의 계약 안에서만 설계한다

### 막 3 — 단일 에이전트 e2e 정합 검토

**병렬 산출물을 병렬로 검토하지 않는다.** 에이전트 하나가 사용자 여정을 처음부터 끝까지
따라 걸으며 계획들이 실제로 엮이는지 본다. 찾는 것: 모순(같은 정책을 다르게) ·
떠넘김(둘 다 "상대가 함") · 중복(둘 다 함) · 구멍(아무도 안 함).

스크립트 검사를 얹을 때는 **정리 전에 먼저 돌려 빨간 것을 보고 나서** 정리한다.
정리 후에 쓴 초록은 "아무것도 안 재는 검사"와 구별되지 않는다.

**탈출 규칙: 검토 2라운드에서 끊는다.** 남은 의심은 재검토하지 말고 실패하는 테스트로
바꿔 큐에 넣는다.

**Why:** 계획 문서를 계획 문서로 검증하면 어느 문장도 실행되지 않아 틀렸다는 증거가 안 생기고,
따라서 수렴하지 않는다. 실제로 3라운드 발견분의 12건이 2라운드 보수가 만든 신규 이음새였다.

## 6. 사용자 이해

계획이 거의 완성되면 사용자가 이해할 시간을 준다.

- **페이지 단위로 나눠** 설명. 한 번에 전부 던지지 않는다
- **교과서처럼 점진 딥다이브** — 쉬운 것부터, 단계마다 깊이를 더한다
- 게이트: 사용자가 이해 완료를 선언. **여기가 마지막 사람 관문**

**Why:** 기획을 사실상 AI 가 다 했으므로, 이해 없이 루프로 들어가면 나중에 결과를 판정할 사람이 없다.

## 7. 루프 구현 (subagent-driven)

상태를 대화가 아니라 디스크에 둔다:

```
joa 플러그인 WORKFLOW.md   ← 방법론 정본 (이 파일, 한 벌)
docs/goal/
  <골>-loop.md             ← 이 골만의 델타. 첫 8줄에 위 파일 참조, 재서술 금지
  fix_plan.md              ← 큐. 남은 일은 `- [ ]` 로 적는다
```

**정본을 레포마다 복사하지 않는다.** 레포 수만큼 정본이 생기면 정본이 아니다.

한 이터레이션 = ① 계획 → ② RED → ③ 서브에이전트 구현 → ④ GREEN + 게이트 →
⑤ 화면 QA → **커밋**. 커밋 메시지가 저널이고 git 히스토리가 기억이다.

- **메인 = 오케스트레이터.** 배치·검증만 한다
- **서브에이전트 = report-only.** 계약 파일·`fix_plan.md` 직접 수정 금지
- 병렬: superpowers 기본은 구현 병렬 금지(conflict). 파일 소유를 사전 배분해 conflict 가
  불가능해지면 허용 — 금지를 어기는 게 아니라 전제를 없애는 것

### ④ 변이 주입 (필수)

GREEN 이 나오면 가드를 되돌려 **다시 빨개지는지** 본다. 안 빨개지면 그 테스트는 아무것도 안 잰다.

### ⑤ 화면 QA — GStack browse 필수

**루프의 QA 는 무조건 GStack `browse`** 로 AI 가 실제 화면을 본다. Playwright 를 따로 깔지 않는다.

```
browse snapshot -i → fill/click → console → network → screenshot
→ 찍은 png 를 Read 로 직접 연다
```

**콘솔 에러 0 · 실패 요청 0 이 아니면 통과가 아니다.** 테스트 초록만으로 QA 통과 판정 금지.

## 검증 대원칙

> **다른 것을 잰 증거를 통과의 근거로 쓰지 않는다.**

화면엔 0글자가 뜨는데 테스트는 초록이었던 사례가 실재한다. 판정하려는 층에서 잰다 —
브라우저가 읽는 값을 단언할 것, 파이프라인 끝의 종료코드를 앞 명령의 것으로 착각하지 말 것.

배선/커버리지 기대값 같은 래칫 수치는 **내려가기만** 한다. 올리기 금지.
